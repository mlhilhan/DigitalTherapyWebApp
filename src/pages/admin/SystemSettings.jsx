import { useState } from "react";
import { Typography, Box } from "@mui/material";

const SystemSettings = () => {
  return (
    <Box>
      <Typography>SystemSettings Page</Typography>
    </Box>
  );
};

export default SystemSettings;
