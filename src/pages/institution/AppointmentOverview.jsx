import { useState } from "react";
import { Typography, Box } from "@mui/material";

const AppointmentOverview = () => {
  return (
    <Box>
      <Typography>AppointmentOverview Page</Typography>
    </Box>
  );
};

export default AppointmentOverview;
