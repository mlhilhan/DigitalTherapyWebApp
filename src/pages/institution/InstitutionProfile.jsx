import { useState } from "react";
import { Typography, Box } from "@mui/material";

const InstitutionProfile = () => {
  return (
    <Box>
      <Typography>InstitutionProfile Page</Typography>
    </Box>
  );
};

export default InstitutionProfile;
