import { useState } from "react";
import { Typography, Box } from "@mui/material";

const AppointmentCalendar = () => {
  return (
    <Box>
      <Typography>AppointmentCalendar Page</Typography>
    </Box>
  );
};

export default AppointmentCalendar;
