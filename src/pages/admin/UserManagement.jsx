import { useState } from "react";
import { Typography, Box } from "@mui/material";

const UserManagement = () => {
  return (
    <Box>
      <Typography>UserManagement Page</Typography>
    </Box>
  );
};

export default UserManagement;
