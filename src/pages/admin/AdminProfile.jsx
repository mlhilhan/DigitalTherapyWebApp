import { useState } from "react";
import { Typography, Box } from "@mui/material";

const AdminProfile = () => {
  return (
    <Box>
      <Typography>AdminProfile Page</Typography>
    </Box>
  );
};

export default AdminProfile;
