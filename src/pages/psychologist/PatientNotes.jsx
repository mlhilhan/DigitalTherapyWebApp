import { useState } from "react";
import { Typography, Box } from "@mui/material";

const PatientNotes = () => {
  return (
    <Box>
      <Typography>PatientNotes Page</Typography>
    </Box>
  );
};

export default PatientNotes;
