import { useState } from "react";
import { Typography, Box } from "@mui/material";

const PatientManagement = () => {
  return (
    <Box>
      <Typography>PatientManagement Page</Typography>
    </Box>
  );
};

export default PatientManagement;
