import { useState } from "react";
import { Typography, Box } from "@mui/material";

const InstitutionManagement = () => {
  return (
    <Box>
      <Typography>InstitutionManagement Page</Typography>
    </Box>
  );
};

export default InstitutionManagement;
